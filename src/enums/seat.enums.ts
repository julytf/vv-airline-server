export enum SeatClasses {
  ECONOMY = 'ECONOMY',
  BUSINESS = 'BUSINESS',
}

export enum SeatTypes {
  NORMAL = 'NORMAL',
  WINDOW = 'WINDOW',
  AISLE = 'AISLE',
}

export enum SeatStatuses {
  AVAILABLE = 'AVAILABLE',
  UNAVAILABLE = 'UNAVAILABLE',
  BOOKED = 'BOOKED',
  BLOCKED = 'BLOCKED',
}
